

source "https://rubygems.org"

# Use the GitHub Pages gem set (pins Jekyll/plugins to compatible versions)
gem "github-pages", group: :jekyll_plugins

# Required when using `remote_theme` in _config.yml
gem "jekyll-remote-theme"

# Optional extras (safe for GitHub Pages)
gem "jekyll-seo-tag"
gem "jekyll-sitemap"
